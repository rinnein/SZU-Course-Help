# 更新记录

本文件记录面向用户的主要变化。完整提交历史可在 GitHub 仓库中查看。

## 未发布

暂无。

## 3.4.0 - 2026-08-28

### 修复

- 修复 macOS 等环境中的 `ddddocr 1.6.1` 仅在 `ddddocr.core` 导出新引擎类时，OCR 自动重登录因顶层导入失败而暂停的问题。
- OCR 初始化同时兼容 `ddddocr.core`、顶层新引擎导出和旧版 `DdddOcr` 接口，并在多平台 Release 构建前执行真实引擎冒烟测试。
- 课程目录切换或首次加载时不再自动展开第一门课程，所有课程默认折叠，用户点击后才显示教学班。
- 修复课程查询和提交请求将校区固定为粤海校区的问题；校区现在随目录和每门清单课程完整传递。
- 旧版 SQLite 清单启动时自动补充校区字段，不需要删除数据库或重新添加课程。
- 工作台课程搜索改为在整个目录内过滤并按匹配结果重新分页：不再只在当前页内筛选，翻页会在匹配结果中进行，且不再额外请求学校接口。
- 首次搜索时自动按序读取当前目录全部课程（显示加载进度并按目录缓存），清空关键词后回到原有的服务端分页视图。
- 完整目录缓存改为绑定当前学号与选课批次，并在手动刷新、批次变化、自动重登录成功和抢课任务结束时失效，避免继续显示过期的冲突、满额或已选状态。
- 自动重登录或批次切换后，空闲状态会自动重载当前课程视图；抢课任务运行中则清除旧结果并提示手动刷新，避免全目录请求争用学校接口。
- 学校返回无效总数、中途空页、分页数据不完整、分页期间总数变化或超过安全页数时，不再把截断结果标记为完整目录。

### 改进

- 新增粤海、丽湖、深大附属医院、技术大学、香港和深理光明六校区切换；首次登录采用学校默认校区，之后的刷新与自动重登录保留手动选择。
- 新增只读周课表：按周一至周日、1 至 14 节展示已选课程；逗号连接的多时段以及单双周排课会逐段进入网格，仅缺少可定位星期与节次的课程单独列出。
- 课表按实际重叠时段分栏，并依据课程名称、周次、地点和教师内容动态调整行高，避免长文本被遮挡或省略。
- 校区加入课程目录缓存范围，切换时取消旧请求并清理旧页面，避免跨校区混用课程、满额或冲突状态。
- 全目录读取改为可取消的顺序请求，并在相邻分页请求间加入短暂间隔，降低连续读取对学校接口的压力。
- 搜索分页回归测试新增刷新、批次切换、自动重登录、异常页数等边界，并修复 Windows 中文系统下 Node 输出解码不一致的问题。

## 3.3.1 - 2026-08-28

### 修复

- 修复学校接口间歇返回纯文本 `NullPointer` 时，连续 5 次未知响应过早暂停长时间抢课任务的问题。

### 改进

- 单门课程的连续未知响应保护阈值默认提高为 200；任意一次已知响应都会清零该课程计数。
- 新增 `COURSE_SELECT_UNKNOWN_RESPONSE_LIMIT` 环境变量，允许按需调整阈值；非法值、零和负数自动回退到 200。
- 网络异常、明确未开放、会话过期和终态失败继续使用各自独立的处理逻辑，不受未知响应阈值变化影响。

## 3.3.0 - 2026-08-28

### 修复

- 修复学校批次显示“正选”、但实际时段尚未开放时，将明确的未开放响应误判为未知返回并连续提交的问题。
- 修复连续未知响应达到阈值后把仍可重试课程永久标记为失败的问题。
- 修复选课接口直接抛出会话过期异常时可能被误报为普通网络异常的问题。
- 修复 OCR 运行期间完成手动登录时，旧 OCR 结果可能覆盖最新学校会话的并发问题。
- 修复任务暂停或单门课程失败后，“移除”按钮仍被旧运行状态锁定的问题。
- 修复移除最后一门待处理课程后，后台任务可能停留在无课程可继续的暂停状态。

### 改进

- 明确未开放响应现在只请求一次并自动暂停；满员课程不受未知返回阈值限制。
- 连续未知响应和网络异常改为保护性暂停，保留清单、课程状态和尝试次数。
- 工作台清单新增任务暂停、继续及失败课程重新排队功能。
- 自动 OCR 重登录状态在工作台可见，成功后自动恢复页面与未完成任务，连续失败后等待手动登录再继续。
- 长时间抢课改由单实例守护线程运行，暂停任务不再占用 HTTP 请求生命周期，关闭程序时也无需等待暂停任务结束。
- 继续暂停任务前重新向学校确认批次；若已切换到预选、关闭或无法确认状态，任务保持暂停且不提交选课请求。
- 自动重登录成功后，即使选课批次刷新暂时失败也会保留已恢复的有效会话并继续任务。
- 暂停流程新增请求边界确认：当前学校请求结束前显示“正在暂停”，安全停稳后才开放活动课程移除；已成功或已失败课程可直接移除。
- 移除课程时同步清理实时进度；若待处理队列已清空，后台线程会有序结束。
- 新增学校关闭时段、超过 20 次满员重试、用户暂停/继续、重登录可见状态和 API 控制测试。
- Release 仅保留各平台与源码 ZIP，移除包内清单和独立校验文件。

## 3.2.1 - 2026-07-17

### 修复

- 修复非开放时段无法获取学校验证码时，登录页长期显示“正在获取验证码”的问题。
- 修复验证码失败后仍保留忙碌状态、登录按钮可能误导用户继续提交的问题。
- 修复自动重登录在学校明确关闭验证码接口时仍可能继续 OCR 重试的问题。

### 改进

- 后端新增验证码关闭时段、超时、网络异常、畸形响应和内部异常的稳定错误码。
- 登录页新增有限状态提示、30 秒请求上限、8 秒图片解析上限和明确的手动重试入口。
- 学校明确返回关闭时段时不自动重试；临时 OCR 识别失败仍保留每次最多 50 张验证码的上限。
- 增加关闭时段 API、前端终止状态、桌面与移动端布局的离线回归验证。
- 更新 GitHub Actions 运行时，并在发布包中加入本更新记录。

## 3.2.0 - 2026-07-16

- 提供 Windows x64、macOS arm64、macOS x64 和 Linux x64 原生 Release 包。
- 发布 Markdown 与 PDF 使用手册、跨平台压缩包和脱敏工作台截图。
- 完善阶段门控、课程目录错误恢复、Card Key V3 与 OCR 自动重登录说明。
